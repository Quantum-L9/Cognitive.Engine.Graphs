id: CONTRACT-03
name: Tenant Isolation
layer: chassis
level: MUST
scope:
  paths:
  - engine/**/*.py
preconditions:
- Neo4j query executed
postconditions:
- Query scoped to tenant database
- No cross-tenant reads
docs:
- docs/contracts/SHARED_MODELS.md
verification:
  scanner_rules:
  - SHARED-002
  test: tests/contracts/test_contracts.py::TestContract03TenantIsolation
