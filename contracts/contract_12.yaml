# --- L9_META ---
# l9_schema: 2
# origin: l9-template
# engine: graph
# layer: [contracts]
# tags: [governance, compliance]
# status: active
# --- /L9_META ---
id: CONTRACT-12
name: Domain Spec Source of Truth
layer: engine
level: MUST
scope:
  paths:
  - engine/config/**/*.py
preconditions:
- Matching behavior implemented
postconditions:
- All behavior from domain_spec.yaml via DomainConfig Pydantic
- Never raw YAML or dicts
verification:
  scanner_rules: []
  test: tests/contracts/test_contract_12.py
