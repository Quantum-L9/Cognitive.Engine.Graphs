# --- L9_META ---
# l9_schema: 2
# origin: l9-template
# engine: graph
# layer: [contracts]
# tags: [governance, compliance]
# status: active
# --- /L9_META ---
id: CONTRACT-23
name: Admin Subaction Registration
layer: hardening
level: MUST
scope:
  paths:
  - engine/handlers.py
preconditions:
- New admin subaction added
postconditions:
- snake_case name
- Returns {status, subaction}
- Logs with tenant/trace_id
- Validates with _require_key()
verification:
  scanner_rules: []
  test: tests/contracts/test_contract_23.py
