id: CONTRACT-14
name: NULL Semantics Deterministic
layer: engine
level: MUST
scope:
  paths:
  - engine/gates/**/*.py
preconditions:
- Gate compiled
postconditions:
- 'null_behavior: pass wraps in IS NULL OR predicate'
- 'null_behavior: fail rejects NULL'
docs:
- docs/contracts/FIELD_NAMES.md
- docs/contracts/PYDANTIC_YAML_MAPPING.md
verification:
  scanner_rules: []
  test: tests/contracts/test_contracts.py::TestContract14NullSemantics
