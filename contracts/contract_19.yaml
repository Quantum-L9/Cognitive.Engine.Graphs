# --- L9_META ---
# l9_schema: 2
# origin: l9-template
# engine: graph
# layer: [contracts]
# tags: [governance, compliance]
# status: active
# --- /L9_META ---
id: CONTRACT-19
name: GDS Jobs Declarative
layer: intelligence
level: MUST
scope:
  paths:
  - engine/gds/**/*.py
preconditions:
- GDS algorithm scheduled
postconditions:
- Declared in spec.gds_jobs
- 'Schedule type: cron|manual'
- Projections spec-driven
verification:
  scanner_rules: []
  test: tests/unit/
