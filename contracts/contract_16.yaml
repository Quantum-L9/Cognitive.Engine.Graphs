# --- L9_META ---
# l9_schema: 2
# origin: l9-template
# engine: graph
# layer: [contracts]
# tags: [governance, compliance]
# status: active
# --- /L9_META ---
id: CONTRACT-16
name: File Structure Fixed
layer: engine
level: MUST
scope:
  paths:
  - engine/**/*
preconditions:
- New directory created
postconditions:
- No new top-level directories without architectural approval
verification:
  scanner_rules: []
  test: tests/contracts/test_contract_16.py
