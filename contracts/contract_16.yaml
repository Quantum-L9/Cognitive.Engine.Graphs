id: CONTRACT-16
name: File Structure Fixed
layer: engine
level: MUST
scope:
  paths:
  - engine/**/*
preconditions:
- New directory created
postconditions:
- No new top-level directories without architectural approval
docs:
- docs/contracts/BANNED_PATTERNS.md
verification:
  scanner_rules: []
  test: tests/contracts/test_contracts.py::TestContract16FileStructure
