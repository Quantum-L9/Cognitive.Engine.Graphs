# PlasticOS payload contracts (CEG)

Payload-only schemas for Gate_SDK `TransportPacket`. No PacketEnvelope inheritance.
No transport fields. Improvement proposals lock `direct_mutation: false`.

Native models: `engine.models.payloads`. See ADR-106.
