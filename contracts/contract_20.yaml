# --- L9_META ---
# l9_schema: 2
# origin: l9-template
# engine: graph
# layer: [contracts]
# tags: [governance, compliance]
# status: active
# --- /L9_META ---
id: CONTRACT-20
name: KGE Embeddings
layer: intelligence
level: MUST
scope:
  paths:
  - engine/kge/**/*.py
preconditions:
- KGE scoring dimension used
postconditions:
- CompoundE3D 256-dim
- Domain-specific, never cross-tenant
verification:
  scanner_rules: []
  test: tests/unit/
