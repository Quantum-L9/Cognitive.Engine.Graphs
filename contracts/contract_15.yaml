id: CONTRACT-15
name: Bidirectional Matching
layer: engine
level: MUST
scope:
  paths:
  - engine/gates/**/*.py
preconditions:
- 'Gate with invertible: true'
postconditions:
- candidate_prop and query_param swapped when direction reverses
docs:
- docs/contracts/BIDIRECTIONAL_MATCHING.md
verification:
  scanner_rules: []
  test: tests/contracts/test_contracts.py::TestContract15BidirectionalMatching
