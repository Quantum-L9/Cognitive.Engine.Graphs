# --- L9_META ---
# l9_schema: 2
# origin: l9-template
# engine: graph
# layer: [contracts]
# tags: [governance, compliance]
# status: active
# --- /L9_META ---
id: CONTRACT-15
name: Bidirectional Matching
layer: engine
level: MUST
scope:
  paths:
  - engine/gates/**/*.py
preconditions:
- 'Gate with invertible: true'
postconditions:
- candidate_prop and query_param swapped when direction reverses
verification:
  scanner_rules: []
  test: tests/unit/
